# Tasks: Add IT Asset Location Filter

- [ ] Add `SelectFilter` for `asset_location_id` to `ITAssetResource::table()`
    - **Validation**: Verify that the filter appears in the IT Assets list.
    - **Validation**: Verify that selecting a location correctly filters the assets.
    - **Validation**: Verify that the filter options are preloaded and searchable.
